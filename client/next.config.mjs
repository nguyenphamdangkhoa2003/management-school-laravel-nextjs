/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["https://khachsan2k.id.vn", "via.placeholder.com", "images.pexels.com", "res.cloudinary.com"],
  },

};

export default nextConfig;
